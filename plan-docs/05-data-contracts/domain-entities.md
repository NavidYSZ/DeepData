---
status: leer
last-updated: 2026-05-09
owner:
---

# Domain-Entities (Drizzle-Schema-Übersicht)

Wird in Phase 4 erarbeitet. Übersicht aller persistenten Entitäten in v2: `account`, `user`, `session`, `auth_account`, `verification`, `domain`, `gsc_connection` (Better-Auth-OAuth), Modul-Tabellen pro Modul. Mit Spalten, Constraints, Indexen, FK-Cascades. Source-of-Truth bleibt das Drizzle-Schema in `lib/db/schema/`; diese Doc ist die annotierte Erklärung.
